{
  "name": "simple-netint",
  "version": "1.0.0",
  "scripts": {
    "esbuild": "esbuild --bundle src/main.ts --outdir=dist",
    "dev": "npm run esbuild -- --watch --serve=3000 --servedir=."
  },
  "devDependencies": {
    "esbuild": "^0.25.2",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  }
}
