/** Class for adding a grid on top of the canvas */
class Grid {
    constructor(org, hFrequency, vFrequency, width, height) {
        this.org = org;
        this.hFrequency = hFrequency;
        this.vFrequency = vFrequency;
        this.width = width;
        this.height = height;
    }

    show(canvas) {
        for (let i = 0; i < this.width; i += this.hrequency) {
            const element = this.width;
        }
    }
}